# Called by ~/.profile by:
# [[ -s "$HOME/<location>/.fett_profile" ]] && source "$HOME/<location>/.fett_profile"
# at the and of ~/.profile
# by mschertel@gmail.com for ubuntu

# Add custom bin directory
[[ -d "$HOME/devel/dotfiles/bin" ]] && PATH="$HOME/devel/dotfiles/bin:$PATH"

# Pip will only work with virtualenv
#export PIP_REQUIRE_VIRTUALENV=true
# and will automatically intall in the active one
#export PIP_RESPECT_VIRTUALENV=true

# Devel - Python / Ruby
[[ -s "$HOME/devel/dotfiles/bash/.fett_devel" ]] && source "$HOME/devel/dotfiles/bash/.fett_devel"
